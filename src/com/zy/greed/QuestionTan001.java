package com.zy.greed;

public class QuestionTan001 {
    /**
     * 给定数字A，B（A < B）和系数P，Q，每次操作可以A = A + P，也可以P = P * Q
     * 至少几次操作可以让 A > B
     *
     * 输入：第一行数据组数T，对于每行数据，共4个 A,B,P,Q
     * A,P >= 1, B <= 10^9, 2 <= Q <= 10, 1 <= T <= 5
     */
}
